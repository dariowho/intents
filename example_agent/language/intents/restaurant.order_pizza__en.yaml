examples:
  - I want a $pizza_type{Margherita}
  - I'd like to have a pizza $pizza_type{diavola}
  - $amount{1} $pizza_type{pepperoni} pizza please

responses:
  default:
    - text:
      - Alright, $amount $pizza_type pizza for you
      - Got it, I will order $amount $pizza_type pizza

#     - custom: $pizza_card  # pizza_card is an Entity that can be serialized as a custom payload

#     - pizza_card: $pizza_card # pizza card is a custom response that takes values from an entity
    
#     - pizza_card:
#         name: $pizza_type
#         image: https://mycdn.com/$pizza_type.png

#     - image: $image_url

#     - custom: $pizza_type # -> {"entity_cls": ''} 
#                           # -> {"entity": PizzaType('margherita')}
#                           # -> PizzaType('margherita')


# restaurant.order_pizza(card=PizzaCard(image_url='...'))