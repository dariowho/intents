examples:
  - Quanto fa $first_operand{2} $operator{times} $second_operand{3}?
  - Sapresti calcolare $first_operand{due} $operator{-} $second_operand{1}?
  - Dimmi il risultato di $first_operand{6} $operator{divided by} $second_operand{tre}

# This intent uses a webhook to produce an answer: if User sees these responses,
# it means that something went wrong in the webhook call
responses:
  default:
    - text:
      - Spiacente, sembra esserci stato un problema tecnico. La tua richiesta non puo' essere completata.
